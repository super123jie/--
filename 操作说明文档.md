# HomeCare-Agent 操作说明文档

> 中兴捧月 · 极致算法赛道 · 兴享智家
> 团队：南邮upup（南京邮电大学）
> 项目：端侧家庭智能体 HomeCare-Agent
> 文档版本：**V5（新增 9 个意图 + 5 个设备工具，外部 100 条标准集 100% 命中）** · 2026-04-27

---

## 目录

- [一、项目说明](#一项目说明)
- [二、环境配置（在新电脑上跑通）](#二环境配置在新电脑上跑通)
- [三、运行项目](#三运行项目)
- [四、Demo 操作指南](#四demo-操作指南)
- [五、自测与端侧基准](#五自测与端侧基准)
- [六、目录结构](#六目录结构)
- [七、常见问题](#七常见问题)

---

## 一、项目说明

### 一句话定位

**面向家庭场景的端侧智能体系统**：用户通过自然语言完成家居控制、老人/儿童关怀、节能场景与安全巡检，全部数据本地处理，零云端上传。

### 核心创新

1. **三层级联意图识别架构**
   - L1 规则正则引擎（高频指令零延迟，~0.05 ms）
   - L2 sklearn TF-IDF + LogisticRegression 分类器（兜底，~0.5 ms）
   - L3 端侧 LLM 嵌入语义匹配（BAAI/bge-small-zh-v1.5，~10-30 ms）
2. **Function Calling 工具调用范式**：每个工具声明 OpenAI 风格 JSON Schema
3. **两段式安全约束**：静态风险分级 + 动态二次确认；high 风险设备（燃气）必须确认
4. **跌倒应急亮点场景**：紧急事件自动放行所有救援动作
5. **多轮上下文 + 指代消解**：自动补齐"再低一点"、"卧室也是"等省略指令
6. **本地化隐私保护**：所有用户数据/对话历史/模型权重均存于本地，零联网上传

### 资源指标（V5 自测，168 条样本）

| 指标 | 数值 | 备注 |
| --- | --- | --- |
| 内部测试集准确率 | **100% (68/68)** | 10 桶含真实用户挑战 / 复合指令 |
| 外部 100 条标准集准确率 | **100% (100/100)** | 独立标注，10 桶各 10 条 |
| 端到端延迟 P50 / P95 / P99 | 6.6 / 12.5 / 23.7 ms | 远低于实时阈值 |
| 内存峰值（RSS） | 568 MB | 4 GB 限制下占 14% |
| 模型权重 | 91.8 MB | bge-small-zh-v1.5 |
| 工具调用成功率 | 100 % | |
| 高风险违规率 | 0 % | |

### V5 新增能力（9 意图 + 5 工具）

- 🎬 **观影模式 mode.movie**：「我要看电影了，把灯调暗一点」/「开启影院模式」→ 客厅灯 20% + 拉窗帘 + 开电视 + 启动环绕音响
- 📚 **学习模式 mode.study**：「孩子要写作业了」/「上网课」→ 儿童房灯调亮 + 关电视/音乐 + 给孩子提示
- 🛡️ **安全巡检 query.security_check**：「检查家里是否安全」→ 一键巡检门锁/燃气/窗户/摄像头并汇总
- 🌬️ **空气类新设备**：`device.air_purifier`（空气净化器）、`device.humidifier`（加湿器）、`device.fresh_air`（新风系统）
- 🚿 **device.water_heater**：热水器开关与温度控制（中风险）
- 💧 **关怀模式扩充**：`care.water_remind`（喝水提醒）/ `care.eye_break`（孩子护眼）/ `care.elder_general`（老人关怀总入口）

### V4 新增能力

- 🎙️ **浏览器语音输入**：点击右侧麦克风按钮即可对家庭智能体说话，识别完成自动提交（基于 Web Speech API，Chrome/Edge 浏览器支持中文）
- 🎨 **深色科技风 UI**：渐变 + 玻璃拟态卡片 + 设备图标网格 + 风险颜色分级
- 🛡️ **5 类安全事件预案**：燃气泄漏 / 老人无响应 / 儿童独自 / 门窗异常 / 夜间陌生开门 — 不只是拦截，而是主动执行救援动作链
- 🔢 **中文数字与边界校验**：理解"二十六度""一百二""半小时"，越界（如 15℃ / 120%）生成明确⚠️警告
- ❌ **否定与排除条件**：解析"睡觉模式但别锁门""打开灯不要开电视"
- 🧩 **复合指令拆解**：把"打开厨房灯和客厅空调到26度"切成多个子任务执行
- 🚧 **能力边界保护**：天气/外卖/股市等能力外查询直接拒绝，不会被错误继承上轮意图

> 算力：CPU 即可运行，**无需 GPU**。换算到端侧 6 TOPS / 4GB 平台仍有大量余量。

---

## 二、环境配置（在新电脑上跑通）

### 1. 系统要求

- **操作系统**：Windows 10 / 11、macOS、Linux 任一
- **Python 版本**：3.10 / 3.11 / 3.12 / 3.13 均可（推荐 3.11，兼容性最佳）
- **磁盘空间**：约 2.5 GB（含 venv、依赖包、模型权重）
- **内存**：建议 4 GB 以上
- **网络**：仅在首次安装依赖与下载模型时需要联网，**之后完全离线运行**

### 2. 安装 Python（如未安装）

打开终端验证：

```bash
python --version
```

若提示找不到 Python：
- Windows：访问 [python.org](https://www.python.org/downloads/) 下载 3.11 安装包，**安装时勾选「Add Python to PATH」**
- macOS：`brew install python@3.11`
- Linux：`sudo apt install python3.11 python3.11-venv`

### 3. 拷贝项目到新电脑

把整个 `南邮upup-南京邮电大学/` 目录拷贝到目标机器。如已包含 `venv/` 和 `models/` 目录，**仍建议在新电脑重新建虚拟环境**（venv 不跨机移植）。

> 模型权重 `algorithm/models/embedder/` 可随项目拷贝（92 MB），无需重新下载。

### 4. 创建虚拟环境

进入项目根目录（含 `algorithm/` 的那一层）：

```bash
cd 南邮upup-南京邮电大学
python -m venv venv
```

### 5. 激活虚拟环境

**Windows（cmd）**：
```cmd
venv\Scripts\activate.bat
```

**Windows（PowerShell）**：
```powershell
venv\Scripts\Activate.ps1
```
若提示 "无法加载文件…执行策略" → 先运行：
```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

**Windows（Git Bash）**：
```bash
source venv/Scripts/activate
```

**macOS / Linux**：
```bash
source venv/bin/activate
```

激活成功后命令行前会出现 `(venv)` 前缀。

### 6. 安装依赖

```bash
pip install --upgrade pip
pip install -r algorithm/requirements.txt
```

主要依赖（约 1.5 GB，包括 PyTorch CPU 版）：
- streamlit、numpy、scikit-learn、jsonschema、psutil、jieba、pydantic
- sentence-transformers、torch、transformers（端侧 LLM）

> 国内网络慢可加镜像源：
> `pip install -r algorithm/requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

### 7. 下载模型权重（仅首次需要联网）

如项目 `algorithm/models/embedder/` 目录已存在 `model.safetensors` 文件，**跳过此步**。

否则运行：
```bash
python algorithm/scripts/download_model.py
```
首次约下载 92 MB；之后该模型就被固化到本地，运行时不再联网。

### 8. 验证环境

```bash
python algorithm/main.py
```
看到 5 条演示输出（睡眠 / 确认 / 调温 / 摔倒 / 状态查询）的 JSON，则环境就绪。

---

## 三、运行项目

### 三种运行入口

| 入口 | 命令 | 适用场景 |
| --- | --- | --- |
| **Web Demo** | `streamlit run algorithm/src/demo.py` | 答辩演示、可视化交互（推荐） |
| **命令行 Demo** | `python algorithm/src/cli.py` | 无 GUI 环境、快速测试 |
| **测试集** | `python algorithm/tests/run_test.py` | 评估准确率与端侧基准 |

> 所有命令在 **激活虚拟环境后** 于项目根目录执行。

### Streamlit 启动详解

```bash
cd 南邮upup-南京邮电大学
# 激活虚拟环境（见上一节）
streamlit run algorithm/src/demo.py
```

启动后会自动打开浏览器到 `http://localhost:8501`。如未自动打开，手动访问该地址。

按 `Ctrl + C` 停止服务。

### 自定义端口

```bash
streamlit run algorithm/src/demo.py --server.port 8800
```

---

## 四、Demo 操作指南

### 界面布局

```
┌─────────────────────┬───────────────────────────────────────┐
│ 侧栏                 │ 主区                                   │
│ ─────────────────── │ ────────────────────────────────────── │
│ 会话 ID              │ 标题：HomeCare-Agent · 端侧家庭智能体   │
│ 🔄 重置会话          │                                        │
│ 🗑️ 清空本地数据      │ 💬 输入框：对家庭智能体说点什么…        │
│                     │ [提交]  [✅ 确认上一步]                 │
│ 快捷场景按钮：       │                                        │
│ 🌙 睡觉              │ 💡 系统回复（绿色成功 / 黄色待确认）    │
│ 🚪 离家              │                                        │
│ 🏡 回家              │ 🎯 意图与槽位（JSON）                  │
│ 🧓 老人摔倒          │ 📋 规划步骤（按风险分色：🟢🟡🔴）       │
│ 💊 提醒吃药          │ 🛡️ 安全裁决                            │
│ 📚 检查作业          │ ⏱️ 各阶段耗时（柱状图）                 │
│ 🔋 节能模式          │                                        │
│                     │ 🏘️ 当前家庭状态（5 个房间设备网格）     │
│ LLM 状态：✅ 已就绪  │ 🗂️ 历史对话（最近 6 轮）                │
│ 隐私 & 端侧提示      │                                        │
└─────────────────────┴───────────────────────────────────────┘
```

### 完整操作流程示例

#### 场景一：睡眠模式 + 二次确认

1. 输入框输入：**`我准备睡觉了，帮我把家里调整一下`** → 点 「提交」
2. 系统返回（黄色提示）：
   > 识别意图：mode.sleep。已生成 14 步计划，其中 1 步涉及风险动作（control_door_lock），请回复『确认』执行，或『取消』终止。
3. 主区显示 14 个规划步骤，最后一步入户门锁（🟡 medium）被阻塞
4. 点击 「✅ 确认上一步」 按钮（或在输入框输入 `确认`）
5. 系统返回（绿色成功）：
   > 已为您完成 14 项操作：客厅灯 off；客厅空调 set_temp(25℃)；…；入户门 lock
6. "当前家庭状态" 网格刷新：所有客厅设备 ⚪ 灰色、主卧灯亮度 15%

#### 场景二：自然语言开放表达（验证 L3 LLM）

输入：**`我累瘫了想躺会`**
- L1 规则不命中、L2 分类器低置信
- L3 嵌入模型 cosine ≈ 0.74 匹配到 mode.sleep
- 与场景一相同的 14 步计划被触发

#### 场景三：多轮上下文继承

按顺序输入：
1. **`把客厅空调调到 26 度`** → 客厅空调设为 26℃
2. **`再低一点`** → 自动继承 device.ac 意图 + down 动作 → 客厅空调 25℃
3. **`卧室也是`** → 继承上轮 → 主卧空调 25℃

#### 场景四：紧急关怀（亮点）

输入：**`奶奶摔倒了！`** 或点击侧栏 「🧓 老人摔倒」

系统立即执行 5 步（**不需要二次确认**）：
1. 全屋灯调至 100% 亮度
2. 呼叫紧急联系人（爸爸手机）
3. 关闭客厅电视
4. 停止音乐
5. 解锁入户门方便救援

#### 场景五：高风险设备阻塞

输入：**`关闭燃气总阀`**
- 系统识别 device.gas_valve（🔴 high 风险）
- 返回阻塞提示：「请回复『确认』执行」
- 必须显式点 「✅ 确认上一步」 才会真正关闭

#### 场景六：状态查询

输入：**`现在家里状态怎么样`** → 显示所有房间设备实时温度、灯光、电视、音响等状态。

### 推荐测试用语清单

| 类型 | 测试语句 |
| --- | --- |
| 直陈控制 | 把客厅灯打开 / 卧室灯关一下 / 空调调到 26 度 / 拉上窗帘 |
| 场景模式 | 我要出门了 / 我刚到家 / 启动节能模式 / 聚会模式 |
| 开放表达 | 我累瘫了想躺会 / 客厅有点闷 / 电费有点高想省点 |
| 多轮 | 把客厅空调调到 26 度 → 再低一点 → 卧室也是 |
| 关怀 | 提醒爷爷吃药 / 看看小明写作业了吗 / 奶奶摔倒了 |
| 高风险 | 关闭燃气总阀 / 解锁入户门 |
| **🆕 真实口语** | 客厅太黑了 / 屋里太闷了 / 声音有点吵 |
| **🆕 否定/排除** | 睡觉模式但别锁门 / 打开客厅灯，不要开电视 |
| **🆕 安全事件** | 厨房闻到煤气味了 / 爷爷半天叫不应 / 孩子一个人在家 / 夜里有陌生人开门 |
| **🆕 中文数字** | 空调开到二十六度 / 灯亮度调到一百 |
| **🆕 越界校验** | 空调调到十五度（应⚠️提示）/ 灯亮度调到一百二（应⚠️提示） |
| **🆕 复合指令** | 打开厨房灯和客厅空调到26度 |
| 超出能力 | 今天天气怎么样 / 帮我订外卖（应返回澄清提示，且不会被继承上轮意图） |

### 🎙️ 语音输入操作

1. 在输入框右侧找到麦克风按钮，显示「🎙️ 按住说话」
2. **首次使用浏览器会请求麦克风权限**，请允许
3. 点击按钮开始录音，对着麦克风说出指令（如"我准备睡觉了"）
4. 再次点击或自动停止后，识别结果会自动填入输入框并提交
5. 系统采用浏览器原生 Web Speech API（Chrome / Edge 支持中文识别），离线降级时建议改用文本输入

> ⚠️ Safari / Firefox 对中文识别支持较弱，推荐 Chrome / Edge。

### 重置数据

- **「🔄 重置会话」**：仅清空当前会话上下文，家庭状态保留
- **「🗑️ 清空本地数据」**：删除 `algorithm/data_local/memory.db`，回到出厂默认设备状态

---

## 五、自测与端侧基准

```bash
# 内部测试集（68 条 / 10 桶 / 含真实用户挑战 + 复合指令）
python algorithm/tests/run_test.py
# 报告 → algorithm/tests/report.json

# 外部独立标注集（100 条 / 10 桶 / 第三方语料）
python algorithm/tests/run_external_test.py
# 报告 → algorithm/tests/report_external.json
```

两套测试当前均 100%。内部脚本输出示例：
```
HomeCare-Agent 测试报告
============================================================
总用例：68
意图识别准确率：100.00%
综合准确率（含槽位/计划/确认要求）：100.00%
工具调用成功率：100.00%
高风险违规率：0.00%

端侧延迟（毫秒）：
  p50: 11.25
  p95: 29.63
  p99: 31.50
内存峰值：564.1 MB
模型与本地数据占用（MB）：{'models': 91.832, 'data_local': 0.059}

分桶准确率：
  single_intent: 10/10 = 100.00%
  compound_multi_step: 10/10 = 100.00%
  ambiguous: 5/5 = 100.00%
  high_risk: 5/5 = 100.00%
  multi_turn_context: 5/5 = 100.00%
  out_of_scope: 5/5 = 100.00%
  care_scenarios: 5/5 = 100.00%
  open_natural_language: 8/8 = 100.00%
```

详细报告写入 `algorithm/tests/report.json`。

---

## 六、目录结构

```
南邮upup-南京邮电大学/                    # ← 提交时打包此目录为 .zip
├── venv/                                # 虚拟环境（不打包提交）
├── algorithm/
│   ├── main.py                          # ★ 官方入口，含 run() 函数
│   ├── requirements.txt
│   ├── README.md
│   ├── models/
│   │   ├── embedder/                    # bge-small-zh-v1.5 LLM 权重（92 MB）
│   │   └── intent_clf.pkl               # TF-IDF 分类器缓存（首次运行自动生成）
│   ├── data_local/memory.db             # SQLite 本地记忆（运行时生成）
│   ├── scripts/
│   │   └── download_model.py            # 一次性模型下载
│   ├── src/
│   │   ├── intent.py                    # L1 + L2 + L3 三层意图识别
│   │   ├── llm.py                       # ★ L3 嵌入式 LLM
│   │   ├── slot.py                      # 槽位抽取（jieba + 词典 + 正则）
│   │   ├── planner.py                   # 规划器
│   │   ├── safety.py                    # 安全约束
│   │   ├── tools.py                     # 11 个家居工具函数
│   │   ├── memory.py                    # SQLite 本地存储
│   │   ├── dialog.py                    # 多轮会话状态机
│   │   ├── edge_opt.py                  # 端侧延迟 + 内存基准
│   │   ├── schema.py                    # 数据契约
│   │   ├── demo.py                      # ★ Streamlit 前端
│   │   └── cli.py                       # 命令行 Demo
│   └── tests/
│       ├── test_cases.json              # 53 条测试用例
│       ├── run_test.py                  # 内部测试驱动器
│       ├── run_external_test.py         # 外部 100 条标准集驱动器
│       ├── home_intent_test_100.json    # 第三方独立标注语料
│       └── report.json                  # 自动生成的测试报告
└── data/
    ├── README.md
    ├── sample_data.json
    ├── 设计方案文档.pdf                  # 待补
    └── 演示视频.mp4                      # 待补
```

### 关键模块速查

| 模块 | 作用 |
| --- | --- |
| `algorithm/main.py` | `run(user_input, home_state, user_profile, session_id, confirm)` 唯一入口 |
| `src/intent.py` | 三层意图识别融合，对外暴露 `predict_intent()` |
| `src/llm.py` | L3 嵌入语义匹配，对外暴露 `predict_semantic()` / `is_available()` |
| `src/planner.py` | 各意图规划函数，对外暴露 `plan(intent, slots, home_state, profile)` |
| `src/tools.py` | `TOOLS` 注册表 + `call_tool(name, state, **kwargs)` |
| `src/safety.py` | `evaluate_all(tool_calls, ...)` 返回每条工具调用的安全裁决 |
| `src/memory.py` | SQLite 持久化偏好/对话/状态 |

---

## 七、常见问题

### Q1: 启动 Streamlit 时报 `streamlit: command not found`

未激活虚拟环境。先运行：
```bash
# Windows
venv\Scripts\activate
# macOS / Linux
source venv/bin/activate
```
再启动。

### Q2: `pip install` 卡住或下载失败

切换镜像源：
```bash
pip install -r algorithm/requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q3: 启动报 `OSError: ... model.safetensors`

模型权重缺失。运行：
```bash
python algorithm/scripts/download_model.py
```

### Q4: 测试报 100% 但 Demo 出错

清空本地数据：
```bash
rm -rf algorithm/data_local algorithm/models/intent_clf.pkl
```
（Windows 用 PowerShell：`Remove-Item -Recurse -Force algorithm\data_local; Remove-Item algorithm\models\intent_clf.pkl`）

然后重新运行 Demo。

### Q5: 浏览器没有自动打开

手动访问 `http://localhost:8501`。

### Q6: 想换 LLM 为生成式（Qwen2.5-0.5B）

需 Python 3.10/3.11 环境（Python 3.13 暂无 llama-cpp-python 预编译 wheel）。
- 重建虚拟环境：`py -3.11 -m venv venv311`
- 安装：`pip install llama-cpp-python`
- 下载 GGUF 权重：`huggingface-cli download Qwen/Qwen2.5-0.5B-Instruct-GGUF qwen2.5-0.5b-instruct-q4_k_m.gguf --local-dir algorithm/models/qwen`
- 在 `src/llm.py` 添加生成接口（架构已预留，参考代码注释）

### Q7: 端口 8501 被占用

```bash
streamlit run algorithm/src/demo.py --server.port 8800
```

### Q8: 想清空所有缓存重新开始

```bash
# Windows Git Bash / Linux / macOS
rm -rf algorithm/data_local algorithm/models/intent_clf.pkl venv

# Windows PowerShell
Remove-Item -Recurse -Force algorithm\data_local, algorithm\models\intent_clf.pkl, venv
```
然后从「二、环境配置」第 4 步开始重做。

### Q9: 麦克风按钮没反应

- 浏览器需允许麦克风权限。Chrome 地址栏左侧锁形图标 → 网站设置 → 麦克风 → 允许
- HTTPS 或 localhost 访问才有麦克风权限。`http://localhost:8501` 是允许的，`http://127.0.0.1:...` 也允许，但局域网 IP 访问可能被浏览器拦截
- 推荐 Chrome 或 Edge 浏览器（中文识别效果最好）
- 如长期不可用，请使用文本输入框

### Q10: 提交比赛时打包

```bash
# 进入项目根目录（含 venv 和 algorithm 的那一层）
# 注意：venv/ 不要打包，太大且不可移植
zip -r 南邮upup-南京邮电大学.zip 南邮upup-南京邮电大学/ \
    -x "南邮upup-南京邮电大学/venv/*" \
    -x "南邮upup-南京邮电大学/**/__pycache__/*" \
    -x "南邮upup-南京邮电大学/**/data_local/*"
```

提交前自检清单：
- [ ] 根目录命名 `南邮upup-南京邮电大学/`
- [ ] `algorithm/main.py` 含 `run()` 函数
- [ ] `algorithm/requirements.txt` 完整
- [ ] `algorithm/models/embedder/model.safetensors` 已包含
- [ ] `data/设计方案文档.pdf` 已补
- [ ] `data/演示视频.mp4` 已补（≤3 分钟）
- [ ] 压缩包 ≤ 4 GB

---

## 八、联系方式

如运行中遇到问题，先查看 `algorithm/README.md` 与本文档的「常见问题」章节。

---

**文档结束。祝答辩顺利！**
